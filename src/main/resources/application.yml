spring:
  application:
    name: spring-ai-rag
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      embedding:
        options:
          model: text-embedding-3-small

sfg:
  aiapp:
    vectorStorePath: /home/intercont/IgorProjects/spring-ai-course/spring-ai-rag/vectorstore-embedding-model-3-small.json
    documentsToLoad:
      - classpath:/movies500.csv

